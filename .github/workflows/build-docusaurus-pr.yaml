name: Build Docusaurus PR

on:
  pull_request:
jobs:
  docusaurus-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: DavidAnson/markdownlint-cli2-action@ced27be96cbfca9963b6a82de3dd05a7ee8a1d3e # v22.0.0
        with:
          globs: |
            **/*.md
            **/*.mdx

      - uses: actions/setup-node@v6
        with:
          node-version: 24.x

      - run: yarn set version berry
      - run: yarn install --immutable
      - run: yarn run build
